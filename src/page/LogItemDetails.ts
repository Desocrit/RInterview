export interface LogItemDetails {
    date: Date;
    author: string;
    leftOperand: number;
    operator: string;
    rightOperand: number;
    result: number;
}

export default LogItemDetails;